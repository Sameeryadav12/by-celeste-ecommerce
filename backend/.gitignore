node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Uploaded images (keep folders via .gitkeep)
uploads/products/*
!uploads/products/.gitkeep
uploads/events/*
!uploads/events/.gitkeep
